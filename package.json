{
  "name": "tournament-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd web; npm install -g bower grunt-cli; npm install; bower install; grunt clean build",
    "prestart": "mongo tournament-auto-tests scripts/testdb-seed.js",
    "start": "JWT_EXPIRES_IN_MINUTES=600 JWT_SECRET_KEY=sdfgsdfg STBGFC_MONGO_URI=mongodb://localhost/tournament-auto-tests NODE_ENV=development node server/app.js",
    "pretest": "mongo tournament-auto-tests scripts/testdb-seed.js",
    "test": "JWT_EXPIRES_IN_MINUTES=600 JWT_SECRET_KEY=sdfgsdfg STBGFC_MONGO_URI=mongodb://localhost/tournament-auto-tests NODE_ENV=test PORT=3001 mocha --exit -s 150ms ./test/api.js"
  },
  "dependencies": {
    "acl": "^0.4.11",
    "baucis": "^1.6.8",
    "bcrypt": "^1.0.3",
    "body-parser": "^1.18.2",
    "cookie-parser": "^1.4.3",
    "debug": "^3.1.0",
    "express": "^4.16.3",
    "express-jwt": "^5.3.1",
    "express.io": "^1.1.13",
    "jsonwebtoken": "^8.2.1",
    "log4js": "^2.5.3",
    "mongoose": "4.7.6",
    "morgan": "^1.9.0",
    "serve-favicon": "^2.5.0",
    "socket.io": "~1.7.4"
  },
  "devDependencies": {
    "mocha": "^5.1.1",
    "supertest": "^3.0.0"
  },
  "engines": {
    "node": "8.11.1"
  }
}
